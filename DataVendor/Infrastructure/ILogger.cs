using System;

namespace Infrastructure
{
    public interface ILogger
    {
        //void Error<T>(Exception ex, string message);
        //void Debug<T>(string message);
        //void Info<T>(string message);
    }
}