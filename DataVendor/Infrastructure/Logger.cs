using System;
using NLog;

namespace Infrastructure
{
    //public class Logger : ILogger
    //{
    //    public void Debug<T>(string message)
    //    {
    //        LogManager.GetLogger(nameof(T)).Debug(message);
    //    }

    //    public void Error<T>(Exception ex, string message)
    //    {
    //        LogManager.GetLogger(nameof(T)).Error(ex, message);
    //    }

    //    public void Info<T>(string message)
    //    {
    //        LogManager.GetLogger(nameof(T)).Info(message);
    //    }
    //}
}
